{
  "name": "backend",
  "version": "0.1.0",
  "bin": {
    "backend": "bin/backend.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "vitest run --passWithNoTests",
    "cdk": "cdk",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(eslint later)\" && exit 0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.17.0",
    "aws-cdk": "2.1021.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.848.0",
    "@aws-sdk/lib-dynamodb": "^3.850.0",
    "aws-cdk-lib": "2.205.0",
    "constructs": "^10.0.0"
  }
}