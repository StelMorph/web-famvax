{
  "name": "famvax",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "bootstrap": "npm ci --workspace frontend && npm ci --workspace backend --if-present",
    "lint": "npm -w frontend run lint --if-present && npm -w backend run lint --if-present",
    "test": "npm -w frontend test --if-present && npm -w backend test --if-present",
    "build": "npm -w frontend run build --if-present",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
